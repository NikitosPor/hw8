name: cocktail_app_tests
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.0.0
  http:
  mocktail_image_network: ^0.2.0
  cupertino_icons: ^1.0.0
  flutter_svg: ^0.22.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0
  build_runner:
  json_serializable: ^4.0.3
  test: ^1.16.8
  mockito: # use this package if you want to use mocks instead of using real instances of dependencies

flutter_driver:
    sdk: flutter


flutter:
  uses-material-design: true

